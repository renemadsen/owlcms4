apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "owlcms4.fullname" . }}-test-connection"
  labels:
    {{- include "owlcms4.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "owlcms4.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
